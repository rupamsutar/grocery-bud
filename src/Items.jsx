import React from 'react'

const Items = ({items, removeItem}) => {
  return (
    <div className="items">
        {items.map((item) => {
            
        })}
    </div>
  )
}

export default Items